运行机制：（看撒鱼要见网）
    事件触发线程管理着：事件队列（事件队列中放着的不只是回调！而是事件。js引擎会读取队列中事件对应的回调（阮一峰））

    看那个三角循环的图片：
        事件触发线程管理着：事件队列
        定时器触发线程管理着：webApi里的事件setTimeout:
        http异步请求线程管理着：webApi里面的ajax;


        